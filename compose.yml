services:
  # =========================================================================
  # BACKEND API (Flask)
  # =========================================================================
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: reconraas_api
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
      - FLASK_DEBUG=False
      - STATS_API_KEY=f4ec87486b7e6f8d83e53fdb37d0b77f9734ca1708d78b59c3cd5c58aa8c9b28

    volumes:
      - ./recon_results:/app/recon_results
      - ./logs:/app/logs
      - ./visits.db:/app/visits.db
    networks:
      - recon-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # =========================================================================
  # FRONTEND (React + Nginx)
  # =========================================================================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: reconraas_frontend
    ports:
      - "3000:80"
    environment:
      - REACT_APP_API_BASE=/api
    networks:
      - recon-network
    restart: unless-stopped

  # =========================================================================
  # NGINX REVERSE PROXY
  # =========================================================================
  nginx:
    image: nginx:alpine
    container_name: reconraas_nginx
    ports:
      - "80:80"
      # - "443:443"  # Uncomment for HTTPS with SSL certificates
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      # - /etc/letsencrypt:/etc/nginx/ssl:ro  # Uncomment for SSL certificates
    depends_on:
      - api
      - frontend
    networks:
      - recon-network
    restart: unless-stopped
    healthcheck:
      test:
        ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 10s
      retries: 3

# ============================================================================
# NETWORKS
# ============================================================================
networks:
  recon-network:
    driver: bridge

# ============================================================================
# VOLUMES (Optional - for persistence)
# ============================================================================
# volumes:
#   recon_results:
#   logs:
